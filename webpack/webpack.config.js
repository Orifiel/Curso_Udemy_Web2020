const webpack = require('webpack')

module.exports = {
    mode: 'development', //poodemos trocar o valor de mode para production, assim o arquivo gerado é minimizado
    entry: './src/principal.js'
}